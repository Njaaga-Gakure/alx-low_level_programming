#include "main.h"

/**
 * mul - multiplies two integers
 * @a: first argument
 * @b: second argument
 * Return: Returns the product of two integers
 */

int mul(int a, int b)
{
	int mult = a * b;

	return (mult);
}
