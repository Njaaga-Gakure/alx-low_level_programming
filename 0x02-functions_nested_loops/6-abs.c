#include <stdlib.h>
#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @a: parameter of the _abs function
 * Return: Returns the absolute value of an integer
 */
int _abs(int a)
{
	int result;

	result = abs(a);
	return (result);
}
