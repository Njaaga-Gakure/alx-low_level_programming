#include "main.h"

/**
 * add - add two numbers
 * @x: first parameter of the add function
 * @y: second parameter of the add function
 *
 * Return: returns sum of two integers
 */
int add(int x, int y)
{
	int sum = x + y;

	return (sum);
}
