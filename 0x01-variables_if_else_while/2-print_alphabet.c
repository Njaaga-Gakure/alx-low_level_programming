#include <stdio.h>

/**
 * main - Program execution starts here
 * Return: Returns a 0 when the program is successful
 */
int main(void)
{
	int i;

	for (i = 'a'; i <= 'z'; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);


}
