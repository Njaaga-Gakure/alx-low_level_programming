#include <stdio.h>

/**
 * main - Starts the execution of the program
 * Return: Returns 0 if program is successful
 */
int main(void)
{
	int i;

	for (i = '0'; i <= '9'; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
