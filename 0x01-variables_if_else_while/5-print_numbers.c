#include <stdio.h>

/**
 * main - Program execution starts here
 * Return: Returns a 0 when the program runs successfully
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
