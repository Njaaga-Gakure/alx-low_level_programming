#include <stdio.h>
#include <string.h>

/**
 * main - Starts the execution of the program
 * Return: Returns 0 when program execution is successful
 */
int main(void)
{
	char progQuote[100];

	strcpy(progQuote, "\"Programming is like building a multilingual puzzle");

	puts(progQuote);

	return (0);
}
